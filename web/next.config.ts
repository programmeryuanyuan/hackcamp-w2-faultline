import type { NextConfig } from 'next'

const config: NextConfig = {
  // snapshot.json is served as a static asset from /public
  // no special config needed — Next.js serves /public at root by default
}

export default config
